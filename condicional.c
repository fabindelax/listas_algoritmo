//escreva um algoritmo que receba uma idade e informe se o usuario podera ou não votar


#include<stdio.h>
int main(){
    int idade;
    
        printf("informe a idade desejada");
        scanf("%d", &idade);

        // se (idade >= 16)
        // entao: escreva ("Pode votar!");
        // senao escreva ("Não pode votar!")

        
        if (idade >= 16)
        printf("Pode votar!\n"); // codigo que será executado caso a condição seja Verdadeira
        else printf("Não pode votar!\n"); // codigo que será executado caso a condição seja Falsa 




}

